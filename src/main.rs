mod block;
mod chain;

// use block::{Transaction, Block};

// fn main() {
//     let tran = Transaction::new("001", Some("tejas"));
//     let block = Block::new(Some("0".to_string()), Some(&tran));
//     println!("{:#?}", block);
// }
